const email = document.querySelector('form')

// sign up
function signUp() {
    if (password !== password_check) {
        alert('패스워드가 일치하지 않습니다.');
        return false;
    }
}

// sign in
function signIn() {

}